naftiko: 1.0.0-alpha2
info:
  label: API Reference — subpackage_organizations.subpackage_organizations/memberTags.subpackage_organizations/memberTags/bulk
  description: 'API Reference — subpackage_organizations.subpackage_organizations/memberTags.subpackage_organizations/memberTags/bulk.
    2 operations. Lead operation: ✨ Bulk create organization member tags. Self-contained Naftiko capability covering one Label
    Studio business surface.'
  tags:
  - Label Studio
  - subpackage_organizations.subpackage_organizations/memberTags.subpackage_organizations/memberTags/bulk
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    LABEL_STUDIO_API_KEY: LABEL_STUDIO_API_KEY
capability:
  consumes:
  - type: http
    namespace: label-studio-subpackage-organizations-subpackage-organizations-membertags-subpackage-organizations-membertags-bulk
    baseUri: http://localhost:8000
    description: API Reference — subpackage_organizations.subpackage_organizations/memberTags.subpackage_organizations/memberTags/bulk
      business capability. Self-contained, no shared references.
    resources:
    - name: api-organizations-id-member-tags-bulk
      path: /api/organizations/{id}/member-tags/bulk
      operations:
      - name: post
        method: POST
        description: ✨ Bulk create organization member tags
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: id
          in: path
          type: integer
          description: A unique integer value identifying this organization.
          required: true
        - name: Authorization
          in: header
          type: string
          description: 'The token (or API key) must be passed as a request header. You can find your user token on the User
            Account page in Label Studio. Example: <br><pre><code class='
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
      - name: delete
        method: DELETE
        description: ✨ Bulk delete organization member tags
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: id
          in: path
          type: integer
          description: A unique integer value identifying this organization.
          required: true
        - name: ids
          in: query
          type: string
          description: Tag IDs to delete. Accepts comma-separated values (e.g., `1,2`) or repeated params.
        - name: Authorization
          in: header
          type: string
          description: 'The token (or API key) must be passed as a request header. You can find your user token on the User
            Account page in Label Studio. Example: <br><pre><code class='
          required: true
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.LABEL_STUDIO_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: label-studio-subpackage-organizations-subpackage-organizations-membertags-subpackage-organizations-membertags-bulk-rest
    port: 8080
    description: REST adapter for API Reference — subpackage_organizations.subpackage_organizations/memberTags.subpackage_organizations/memberTags/bulk.
      One Spectral-compliant resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/api/organizations/{id}/member-tags/bulk
      name: api-organizations-id-member-tags-bulk
      description: REST surface for api-organizations-id-member-tags-bulk.
      operations:
      - method: POST
        name: post
        description: ✨ Bulk create organization member tags
        call: label-studio-subpackage-organizations-subpackage-organizations-membertags-subpackage-organizations-membertags-bulk.post
        with:
          id: rest.id
          Authorization: rest.Authorization
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: ✨ Bulk delete organization member tags
        call: label-studio-subpackage-organizations-subpackage-organizations-membertags-subpackage-organizations-membertags-bulk.delete
        with:
          id: rest.id
          ids: rest.ids
          Authorization: rest.Authorization
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: label-studio-subpackage-organizations-subpackage-organizations-membertags-subpackage-organizations-membertags-bulk-mcp
    port: 9090
    transport: http
    description: MCP adapter for API Reference — subpackage_organizations.subpackage_organizations/memberTags.subpackage_organizations/memberTags/bulk.
      One tool per consumed operation, routed inline through this capability's consumes block.
    tools:
    - name: bulk-create-organization-member-tags
      description: ✨ Bulk create organization member tags
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: label-studio-subpackage-organizations-subpackage-organizations-membertags-subpackage-organizations-membertags-bulk.post
      with:
        id: tools.id
        Authorization: tools.Authorization
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: bulk-delete-organization-member-tags
      description: ✨ Bulk delete organization member tags
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: label-studio-subpackage-organizations-subpackage-organizations-membertags-subpackage-organizations-membertags-bulk.delete
      with:
        id: tools.id
        ids: tools.ids
        Authorization: tools.Authorization
      outputParameters:
      - type: object
        mapping: $.
