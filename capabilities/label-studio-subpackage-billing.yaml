naftiko: 1.0.0-alpha2
info:
  label: API Reference — subpackage_billing
  description: 'API Reference — subpackage_billing. 1 operations. Lead operation: ✨ Get billing info. Self-contained Naftiko
    capability covering one Label Studio business surface.'
  tags:
  - Label Studio
  - subpackage_billing
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    LABEL_STUDIO_API_KEY: LABEL_STUDIO_API_KEY
capability:
  consumes:
  - type: http
    namespace: label-studio-subpackage-billing
    baseUri: http://localhost:8000
    description: API Reference — subpackage_billing business capability. Self-contained, no shared references.
    resources:
    - name: api-billing-info
      path: /api/billing/info
      operations:
      - name: info
        method: GET
        description: ✨ Get billing info
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
          description: 'The token (or API key) must be passed as a request header. You can find your user token on the User
            Account page in Label Studio. Example: <br><pre><code class='
          required: true
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.LABEL_STUDIO_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: label-studio-subpackage-billing-rest
    port: 8080
    description: REST adapter for API Reference — subpackage_billing. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/api/billing/info
      name: api-billing-info
      description: REST surface for api-billing-info.
      operations:
      - method: GET
        name: info
        description: ✨ Get billing info
        call: label-studio-subpackage-billing.info
        with:
          Authorization: rest.Authorization
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: label-studio-subpackage-billing-mcp
    port: 9090
    transport: http
    description: MCP adapter for API Reference — subpackage_billing. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: get-billing-info
      description: ✨ Get billing info
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: label-studio-subpackage-billing.info
      with:
        Authorization: tools.Authorization
      outputParameters:
      - type: object
        mapping: $.
