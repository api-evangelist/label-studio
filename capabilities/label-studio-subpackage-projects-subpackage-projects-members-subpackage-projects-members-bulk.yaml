naftiko: 1.0.0-alpha2
info:
  label: API Reference — subpackage_projects.subpackage_projects/members.subpackage_projects/members/bulk
  description: 'API Reference — subpackage_projects.subpackage_projects/members.subpackage_projects/members/bulk. 2 operations.
    Lead operation: ✨ Bulk assign project members. Self-contained Naftiko capability covering one Label Studio business surface.'
  tags:
  - Label Studio
  - subpackage_projects.subpackage_projects/members.subpackage_projects/members/bulk
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    LABEL_STUDIO_API_KEY: LABEL_STUDIO_API_KEY
capability:
  consumes:
  - type: http
    namespace: label-studio-subpackage-projects-subpackage-projects-members-subpackage-projects-members-bulk
    baseUri: http://localhost:8000
    description: API Reference — subpackage_projects.subpackage_projects/members.subpackage_projects/members/bulk business
      capability. Self-contained, no shared references.
    resources:
    - name: api-projects-id-members-bulk
      path: /api/projects/{id}/members/bulk/
      operations:
      - name: post
        method: POST
        description: ✨ Bulk assign project members
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: id
          in: path
          type: integer
          required: true
        - name: last_activity__gte
          in: query
          type: string
          description: Filter by last activity (ISO 8601 formatted date). Only when all=True.
        - name: last_activity__lte
          in: query
          type: string
          description: Filter by last activity upper bound (ISO 8601 formatted date). Only when all=True.
        - name: role
          in: query
          type: string
          description: Filter by role, project roles take precedence over organization roles. Only when all=True. (comma-separated
            values)
        - name: search
          in: query
          type: string
          description: Search term for filtering members by name, email, or username. Only when all=True.
        - name: tags
          in: query
          type: string
          description: Multiple values may be separated by commas. (comma-separated values)
        - name: user_type
          in: query
          type: string
          description: Multiple values may be separated by commas. (comma-separated values)
        - name: Authorization
          in: header
          type: string
          description: 'The token (or API key) must be passed as a request header. You can find your user token on the User
            Account page in Label Studio. Example: <br><pre><code class='
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
      - name: delete
        method: DELETE
        description: ✨ Bulk unassign project members
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: id
          in: path
          type: integer
          required: true
        - name: all
          in: query
          type: boolean
          description: Apply unassignment to all currently matched project members.
        - name: excluded
          in: query
          type: string
          description: Comma-separated list of user IDs to keep assigned when `all=true`.
        - name: included
          in: query
          type: string
          description: Comma-separated list of user IDs to unassign when `all=false`.
        - name: last_activity__gte
          in: query
          type: string
          description: Filter by last activity (ISO 8601 formatted date). Only when all=True.
        - name: last_activity__lte
          in: query
          type: string
          description: Filter by last activity upper bound (ISO 8601 formatted date). Only when all=True.
        - name: role
          in: query
          type: string
          description: Filter by role, project roles take precedence over organization roles. Only when all=True. (comma-separated
            values)
        - name: search
          in: query
          type: string
          description: Search term for filtering members by name, email, or username. Only when all=True.
        - name: tags
          in: query
          type: string
          description: Multiple values may be separated by commas. (comma-separated values)
        - name: user_type
          in: query
          type: string
          description: Multiple values may be separated by commas. (comma-separated values)
        - name: Authorization
          in: header
          type: string
          description: 'The token (or API key) must be passed as a request header. You can find your user token on the User
            Account page in Label Studio. Example: <br><pre><code class='
          required: true
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.LABEL_STUDIO_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: label-studio-subpackage-projects-subpackage-projects-members-subpackage-projects-members-bulk-rest
    port: 8080
    description: REST adapter for API Reference — subpackage_projects.subpackage_projects/members.subpackage_projects/members/bulk.
      One Spectral-compliant resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/api/projects/{id}/members/bulk
      name: api-projects-id-members-bulk
      description: REST surface for api-projects-id-members-bulk.
      operations:
      - method: POST
        name: post
        description: ✨ Bulk assign project members
        call: label-studio-subpackage-projects-subpackage-projects-members-subpackage-projects-members-bulk.post
        with:
          id: rest.id
          last_activity__gte: rest.last_activity__gte
          last_activity__lte: rest.last_activity__lte
          role: rest.role
          search: rest.search
          tags: rest.tags
          user_type: rest.user_type
          Authorization: rest.Authorization
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: ✨ Bulk unassign project members
        call: label-studio-subpackage-projects-subpackage-projects-members-subpackage-projects-members-bulk.delete
        with:
          id: rest.id
          all: rest.all
          excluded: rest.excluded
          included: rest.included
          last_activity__gte: rest.last_activity__gte
          last_activity__lte: rest.last_activity__lte
          role: rest.role
          search: rest.search
          tags: rest.tags
          user_type: rest.user_type
          Authorization: rest.Authorization
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: label-studio-subpackage-projects-subpackage-projects-members-subpackage-projects-members-bulk-mcp
    port: 9090
    transport: http
    description: MCP adapter for API Reference — subpackage_projects.subpackage_projects/members.subpackage_projects/members/bulk.
      One tool per consumed operation, routed inline through this capability's consumes block.
    tools:
    - name: bulk-assign-project-members
      description: ✨ Bulk assign project members
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: label-studio-subpackage-projects-subpackage-projects-members-subpackage-projects-members-bulk.post
      with:
        id: tools.id
        last_activity__gte: tools.last_activity__gte
        last_activity__lte: tools.last_activity__lte
        role: tools.role
        search: tools.search
        tags: tools.tags
        user_type: tools.user_type
        Authorization: tools.Authorization
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: bulk-unassign-project-members
      description: ✨ Bulk unassign project members
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: label-studio-subpackage-projects-subpackage-projects-members-subpackage-projects-members-bulk.delete
      with:
        id: tools.id
        all: tools.all
        excluded: tools.excluded
        included: tools.included
        last_activity__gte: tools.last_activity__gte
        last_activity__lte: tools.last_activity__lte
        role: tools.role
        search: tools.search
        tags: tools.tags
        user_type: tools.user_type
        Authorization: tools.Authorization
      outputParameters:
      - type: object
        mapping: $.
