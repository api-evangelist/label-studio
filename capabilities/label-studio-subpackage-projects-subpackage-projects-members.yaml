naftiko: 1.0.0-alpha2
info:
  label: API Reference — subpackage_projects.subpackage_projects/members
  description: 'API Reference — subpackage_projects.subpackage_projects/members. 2 operations. Lead operation: ✨ Add project
    member. Self-contained Naftiko capability covering one Label Studio business surface.'
  tags:
  - Label Studio
  - subpackage_projects.subpackage_projects/members
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    LABEL_STUDIO_API_KEY: LABEL_STUDIO_API_KEY
capability:
  consumes:
  - type: http
    namespace: label-studio-subpackage-projects-subpackage-projects-members
    baseUri: http://localhost:8000
    description: API Reference — subpackage_projects.subpackage_projects/members business capability. Self-contained, no shared
      references.
    resources:
    - name: api-projects-id-members
      path: /api/projects/{id}/members/
      operations:
      - name: add
        method: POST
        description: ✨ Add project member
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: id
          in: path
          type: integer
          required: true
        - name: Authorization
          in: header
          type: string
          description: 'The token (or API key) must be passed as a request header. You can find your user token on the User
            Account page in Label Studio. Example: <br><pre><code class='
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
      - name: remove
        method: DELETE
        description: ✨ Remove member from project
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: id
          in: path
          type: integer
          required: true
        - name: user
          in: query
          type: integer
          description: User ID to remove from the project. Optional for backward compatibility with DELETE body.
        - name: Authorization
          in: header
          type: string
          description: 'The token (or API key) must be passed as a request header. You can find your user token on the User
            Account page in Label Studio. Example: <br><pre><code class='
          required: true
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.LABEL_STUDIO_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: label-studio-subpackage-projects-subpackage-projects-members-rest
    port: 8080
    description: REST adapter for API Reference — subpackage_projects.subpackage_projects/members. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/api/projects/{id}/members
      name: api-projects-id-members
      description: REST surface for api-projects-id-members.
      operations:
      - method: POST
        name: add
        description: ✨ Add project member
        call: label-studio-subpackage-projects-subpackage-projects-members.add
        with:
          id: rest.id
          Authorization: rest.Authorization
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: remove
        description: ✨ Remove member from project
        call: label-studio-subpackage-projects-subpackage-projects-members.remove
        with:
          id: rest.id
          user: rest.user
          Authorization: rest.Authorization
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: label-studio-subpackage-projects-subpackage-projects-members-mcp
    port: 9090
    transport: http
    description: MCP adapter for API Reference — subpackage_projects.subpackage_projects/members. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: add-project-member
      description: ✨ Add project member
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: label-studio-subpackage-projects-subpackage-projects-members.add
      with:
        id: tools.id
        Authorization: tools.Authorization
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: remove-member-project
      description: ✨ Remove member from project
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: label-studio-subpackage-projects-subpackage-projects-members.remove
      with:
        id: tools.id
        user: tools.user
        Authorization: tools.Authorization
      outputParameters:
      - type: object
        mapping: $.
