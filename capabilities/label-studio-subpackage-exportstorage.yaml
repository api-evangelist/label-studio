naftiko: 1.0.0-alpha2
info:
  label: API Reference — subpackage_exportStorage
  description: 'API Reference — subpackage_exportStorage. 1 operations. Lead operation: List all export storages types. Self-contained
    Naftiko capability covering one Label Studio business surface.'
  tags:
  - Label Studio
  - subpackage_exportStorage
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    LABEL_STUDIO_API_KEY: LABEL_STUDIO_API_KEY
capability:
  consumes:
  - type: http
    namespace: label-studio-subpackage-exportstorage
    baseUri: http://localhost:8000
    description: API Reference — subpackage_exportStorage business capability. Self-contained, no shared references.
    resources:
    - name: api-storages-export-types
      path: /api/storages/export/types
      operations:
      - name: listtypes
        method: GET
        description: List all export storages types
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
          description: 'The token (or API key) must be passed as a request header. You can find your user token on the User
            Account page in Label Studio. Example: <br><pre><code class='
          required: true
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.LABEL_STUDIO_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: label-studio-subpackage-exportstorage-rest
    port: 8080
    description: REST adapter for API Reference — subpackage_exportStorage. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/api/storages/export/types
      name: api-storages-export-types
      description: REST surface for api-storages-export-types.
      operations:
      - method: GET
        name: listtypes
        description: List all export storages types
        call: label-studio-subpackage-exportstorage.listtypes
        with:
          Authorization: rest.Authorization
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: label-studio-subpackage-exportstorage-mcp
    port: 9090
    transport: http
    description: MCP adapter for API Reference — subpackage_exportStorage. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: list-all-export-storages-types
      description: List all export storages types
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: label-studio-subpackage-exportstorage.listtypes
      with:
        Authorization: tools.Authorization
      outputParameters:
      - type: object
        mapping: $.
